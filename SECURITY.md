# WinRemote 安全声明

WinRemote 是一个远程控制插件，作为使用者，你应该了解它的安全边界。

---

## 安全机制

### Token + 密码
- Token SHA-256 哈希存储，明文不可见
- 二次密码可选，5 次错误自动封禁 30 分钟

### 指令白名单
- 默认仅允许 `ipconfig` / `tasklist` / `dir` / `whoami` / `systeminfo` 等安全指令
- 危险操作需手动开白名单

### 会话级临时授权
- 授权默认 5 分钟自动过期
- 永久授权需私聊确认
- 重启后所有授权自动失效

### HMAC 审计日志
- 所有操作写入 `winremote_audit.jsonl`
- HMAC-SHA-256 签名，可独立校验篡改
- `python auth.py <log> <token>` 验证完整性

### 私聊确认
- 高危操作向管理员私聊发送申请
- 回复「同意/确认/yes」→ 通过
- 5 分钟不回复 → 自动取消

### 路径限制
- 文件读写默认锁定 `C:\\Temp`
- 禁止访问系统目录

### 公网熔断
- 检测到公网 IP 连接 → 自动禁用高危功能

---

## 使用说明

这个插件只应该在你自己的电脑上、用自己的 QQ 号控制、在你知道自己在做什么的情况下使用。
